services:
  screener:
    build: .
    image: screener:local
    ports:
      - "8080:8080"
    env_file: .env          # DEEPGRAM_API_KEY, OPENAI_API_KEY, TTS_PROVIDER, ...
    restart: unless-stopped
